package src.poker.card;

public enum Suit {
    /** Encoded as 0001. */
    Spades,
    /** Encoded as 0010. */
    Hearts,
    /** Encoded as 0100. */
    Diamonds,
    /** Encoded as 1000. */
    Clubs
}